[project]
name = "cryptotrade"
version = "0.1.0"
description = "Multi-strategy crypto and sports-arb paper-trading engine with a local dashboard"
requires-python = ">=3.11"

[project.scripts]
cryptotrade = "src.cli:main"
cryptotrade-start = "src.api.server:main"
